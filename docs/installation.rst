============
Installation
============

At the command line::

    $ easy_install drf-impersonate

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv drf-impersonate
    $ pip install drf-impersonate
